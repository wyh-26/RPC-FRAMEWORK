package wyh.rpc.modelTest;

import wyh.rpc.loader.ExtensionLoader;
import wyh.rpc.proxy.ProxyProvider;
import wyh.rpc.service.Talk;
import wyh.rpc.serviceManager.serviceRegistry.ServiceRegistry;
import wyh.rpc.utils.SingletonFactory;

import java.net.InetSocketAddress;

public class ExtensionLoaderTest {
    public static void main(String[] args) {
//        ServiceRegistry serviceRegistry = ExtensionLoader.getExtension(ServiceRegistry.class , "zk");
//        serviceRegistry.findService();
//        serviceRegistry = ExtensionLoader.getExtension(ServiceRegistry.class , "nacos");
//        serviceRegistry.findService();
//        InetSocketAddress inetSocketAddress = new InetSocketAddress("127.0.0.1" , 8080);
//        System.out.println(inetSocketAddress.toString());
//        ProxyProvider proxyProvider = SingletonFactory.getSingleton(ProxyProvider.class);
//        Talk talk = proxyProvider.getRpcClientProxy(Talk.class , );
//        System.out.println(talk.talk());
    }
}
